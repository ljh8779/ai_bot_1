# Domain / TLS
DOMAIN=chat.example.com
ACME_EMAIL=ops@example.com

# Bulk ingestion host directory (Linux server path)
BULK_INGEST_HOST_DIR=/opt/ai_bot_folder

# Database (docker compose service: db)
POSTGRES_DB=rag_db
POSTGRES_USER=rag_user
POSTGRES_PASSWORD=change_this_password

# App
APP_NAME=groupware-rag-bot
APP_ENV=prod

# LLM
LLM_PROVIDER=google
LLM_TIMEOUT_SECONDS=120
LLM_MAX_RETRIES=2
LLM_CHAT_TEMPERATURE=0.2

# Google (recommended for easiest production start)
GOOGLE_BASE_URL=https://generativelanguage.googleapis.com
GOOGLE_API_KEY=replace_with_new_google_key
GOOGLE_EMBEDDING_MODEL=gemini-embedding-001
GOOGLE_CHAT_MODEL=gemini-2.5-flash

# Ollama (only used when LLM_PROVIDER=ollama and local-llm profile enabled)
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_EMBEDDING_MODEL=nomic-embed-text
OLLAMA_CHAT_MODEL=qwen2.5:7b

# SQLAlchemy / DB
DATABASE_URL=postgresql+psycopg://rag_user:change_this_password@db:5432/rag_db
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT_SECONDS=30
DB_POOL_RECYCLE_SECONDS=1800

# RAG tuning
EMBEDDING_DIMENSIONS=768
CHUNK_SIZE=800
CHUNK_OVERLAP=120
INGEST_EMBEDDING_BATCH_SIZE=64
MAX_CONTEXT_CHUNKS=5
SEARCH_CANDIDATE_MULTIPLIER=4
ALLOW_GENERAL_FALLBACK=true
MAX_UPLOAD_SIZE_MB=20

# Bulk ingest
BULK_INGEST_DIR=/bulk_ingest
BULK_INGEST_MAX_FILES=1000
BULK_INGEST_ZIP_MEMBER_LIMIT=2000
BULK_INGEST_DETAILS_LIMIT=300

# OCR
PDF_OCR_ENABLED=true
PDF_OCR_MAX_PAGES=20
PDF_OCR_DPI=200
PDF_OCR_FALLBACK_MIN_CHARS=40
OCR_TESSERACT_LANG=kor+eng
